---
driver:
  name: docker  

platforms:
  - name: ubuntu-jupyter
    image: "docker.io/geerlingguy/docker-ubuntu2404-ansible:latest"
    #image:  "docker.io/ubuntu:22.04"     Не подходит, нет systemd
    #image: "jrei/systemd-ubuntu:22.04"   Не пробовал
    pre_build_image: true
    systemd: true  
    #privileged: true
    command: /sbin/init     #То же, что и строкой ниже, но в общем виде, некоторая обертка над systemd
    #command: "/lib/systemd/systemd"
    #volumes:
     # - /sys/fs/cgroup:/sys/fs/cgroup:rw
    #dns_servers:
     # - 8.8.8.8
      #- 1.1.1.1
    #tmpfs:
     #   /run: ""
      #  /tmp: ""
    env:
      container: docker

#provisioner:
  #name: ansible

#verifier:
  #name: ansible
